#include <stdio.h>
#include <stdbool.h>
#include <stdlib.h>

int solution(const char* str1, const char* str2) {
    int answer = 2;
    
    if(strstr(str1, str2) != NULL) answer = 1;
    return answer;
}